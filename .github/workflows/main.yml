name: CI/CD Pipeline

on:
  push:
<<<<<<< HEAD
    branches:
      - main
      - develop
  pull_request:
=======
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  DOCKER_IMAGE: ${{ secrets.DOCKER_USERNAME }}/todoapp
>>>>>>> 0ab6c93e75e705766a49b85cb754bb65f430f005

jobs:
  # Stage 1: Build & Test
  build-test:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Run Unit Tests
        run: |
          if [ -d "tests" ]; then pytest tests/; fi

  # Stage 2: Lint & Security Scan
  lint-security:
    runs-on: ubuntu-latest
    needs: build-test
    steps:
      - uses: actions/checkout@v3

      - name: Install Linters
        run: pip install flake8 bandit

      - name: Run Linter
        run: flake8 .

      - name: Run Security Scan
        run: bandit -r .

  # Stage 3: Docker Build & Push
  docker-build-push:
    runs-on: ubuntu-latest
    needs: lint-security
    steps:
      - uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/todoapp:latest .

      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/todoapp:latest

  # Stage 4: Deploy App (Ansible + K8s)
  deploy:
    runs-on: ubuntu-latest
    needs: docker-build-push
    steps:
      - uses: actions/checkout@v3

      - name: Install Ansible
        run: sudo apt update && sudo apt install -y ansible

      - name: Run Ansible Playbook
        run: ansible-playbook -i ansible/host.ini ansible/playbook.yaml

      - name: Apply Kubernetes Manifests
        run: |
          if [ -d "k8s" ]; then
            kubectl apply -f k8s/namespace.yaml || true
            kubectl apply -f k8s/configmap.yaml || true
            kubectl apply -f k8s/deployment.yaml || true
            kubectl apply -f k8s/service.yaml || true
          fi

  # Stage 5: Post-Deploy Smoke Tests
  smoke-tests:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - uses: actions/checkout@v3

      - name: Check Pods
        run: kubectl get pods --all-namespaces || true

      - name: Check Services
        run: kubectl get svc --all-namespaces || true
=======
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: python manage.py test --verbosity=2
        env:
          DEBUG: "True"
          SECRET_KEY: "test-secret-key"
          DB_ENGINE: django.db.backends.sqlite3

  # Stage 2: Security & Linting
  lint-security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install linters
        run: pip install flake8 bandit

      - name: Run Flake8 (linting)
        run: flake8 . --max-line-length=120 --exclude=migrations,static --ignore=E501,W503
        continue-on-error: true

      - name: Run Bandit (security)
        run: bandit -r . -x ./migrations,./static --skip B101
        continue-on-error: true

  # Stage 3: Docker Build & Push
  docker:
    needs: [build-test, lint-security]
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE }}:latest
            ${{ env.DOCKER_IMAGE }}:${{ github.sha }}

  # Stage 4: Deploy to Kubernetes
  deploy:
    needs: docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Verify kubectl connection
        run: |
          kubectl cluster-info
          kubectl get nodes

      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f k8s/namespace.yaml
          kubectl apply -f k8s/configmap.yaml
          kubectl apply -f k8s/secret.yaml
          kubectl apply -f k8s/mysql-deployment.yaml
          kubectl apply -f k8s/redis-deployment.yaml
          kubectl apply -f k8s/app-deployment.yaml
          kubectl apply -f k8s/service.yaml

      - name: Wait for deployment
        run: |
          kubectl rollout status deployment/todoapp -n dev --timeout=5m
          kubectl rollout status deployment/mysql -n dev --timeout=5m
          kubectl rollout status deployment/redis -n dev --timeout=5m

  # Stage 5: Smoke Tests
  smoke-test:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Verify Deployment
        run: |
          echo "=== Checking Pods ==="
          kubectl get pods -n dev
          echo ""
          echo "=== Checking Services ==="
          kubectl get svc -n dev
          echo ""
          echo "=== Checking Deployments ==="
          kubectl get deployments -n dev
          echo ""
          echo "✅ Smoke tests completed successfully"

      - name: Check application health
        run: |
          kubectl get pods -n dev -l app=todoapp -o jsonpath='{.items[0].status.phase}' | grep -q "Running" && echo "✅ App is running" || echo "❌ App is not running"
>>>>>>> 0ab6c93e75e705766a49b85cb754bb65f430f005
