apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: dev
data:
  APP_ENV: development
  LOG_LEVEL: info
  DB_HOST: localhost
  DB_PORT: "5432"
  CACHE_HOST: localhost
  CACHE_PORT: "6379"
